<resources xmlns:tools="http://schemas.android.com/tools">
    <!--
      FIX #1: Parent changed from DarkActionBar → NoActionBar.
      We supply our own Toolbar in activity_main.xml, so the system
      must NOT also try to create a second ActionBar — that crashes
      with "This Activity already has an action bar supplied by the
      window decor" on some devices/themes.
    -->
    <style name="Theme.SpeakMate" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/primary_blue</item>
        <item name="colorPrimaryVariant">@color/primary_blue_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="colorSecondary">@color/accent_teal</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="android:colorBackground">@color/bg_surface</item>
        <item name="colorSurface">@color/bg_card</item>
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <!-- Splash screen theme – no action bar, solid blue background -->
    <style name="Theme.SpeakMate.Splash" parent="Theme.SpeakMate">
        <item name="android:windowBackground">@color/primary_blue</item>
        <item name="android:statusBarColor">@color/primary_blue_dark</item>
    </style>

    <!-- Card style helper -->
    <style name="FeatureCard" parent="Widget.MaterialComponents.CardView">
        <item name="cardCornerRadius">16dp</item>
        <item name="cardElevation">3dp</item>
        <item name="android:layout_margin">6dp</item>
    </style>
</resources>
